# Before the v3 CV update

Saved September 21, 2026 before replacing the homepage's CV and resume downloads
with the two v3 PDFs supplied by the user from their Downloads folder.

- `index.html`: homepage before the RepoRevive and LLM test evaluation updates.
- `cv.pdf`, `resume.pdf`: the previous September 9 resume versions.
- `README.md`: the maintenance notes at the time of the snapshot.
- `SHA256SUMS.txt`: SHA-256 hashes of these four original files.

To restore this snapshot, copy those four files back to the repository root.
It shares the existing root-level assets and research attachments; view the
HTML after restoring to the root so relative asset links resolve correctly.
The existing GitHub Pages configuration excludes the entire `_archive` folder.
